// Validate Stack Sequences
// https://leetcode.com/problems/validate-stack-sequences/
// Solved: 2026-07-21T09:22:30.499Z | Attempts before success: 129

            while(!st.empty() && st.top()==popped[j]){
                st.pop();
                j++;
            st.push(pushed[i]);
        for(int i=0;i<pushed.size();i++){
        int j=0;
        stack<int>st;
            }
        }
        return st.empty();         
    }
