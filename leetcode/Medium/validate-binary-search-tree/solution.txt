// Validate Binary Search Tree
// https://leetcode.com/problems/validate-binary-search-tree/
// Solved: 2026-07-18T06:42:01.547Z | Attempts before success: 112

public:
    bool isValidBST(TreeNode* root) {
        return valid(root,LONG_MIN,LONG_MAX);
    }

    bool valid(TreeNode* root,long minV,long maxV){
        if(!root) return true;
    }
        if(root->val >= maxV || root->val <= minV) return false;
        return valid(root->left,minV,root->val) && valid(root->right,root->val,maxV);
class Solution {
